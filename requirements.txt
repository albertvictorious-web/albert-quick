# Manifest dependency untuk Vercel Python Function (api/index.py).
# Vercel membaca requirements.txt di ROOT project — bukan backend/requirements.txt.
#
# Isinya SENGAJA hanya paket runtime. backend/requirements.txt tetap berisi
# tooling development (pytest, black, mypy, dst) yang tidak perlu ikut dibundel.
# pandas/numpy/boto3/emergentintegrations sudah dipastikan TIDAK dipakai kode
# backend (diverifikasi via grep), jadi tidak disertakan — ini yang paling besar
# menghemat ukuran bundle.

fastapi==0.141.1
pydantic>=2.6.4
email-validator>=2.2.0
python-multipart>=0.0.9

# MongoDB Atlas. dnspython WAJIB ada: skema mongodb+srv:// butuh lookup SRV.
motor==3.7.1
pymongo==4.17.0
dnspython>=2.6.1

# Auth: JWT di httpOnly cookie + hash password bcrypt.
# passlib 1.7.4 tidak kompatibel dengan bcrypt>=4.1 — pin ini wajib dipertahankan.
pyjwt>=2.10.1
passlib>=1.7.4
bcrypt==4.0.1

# Fitur Upload Leads (.xlsx / .xlsm / .xls). CSV pakai modul csv bawaan.
openpyxl==3.1.5
xlrd==2.0.1

python-dotenv>=1.0.1
tzdata>=2024.2
